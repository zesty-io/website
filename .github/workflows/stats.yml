name: Zesty Website PR Stats

on: [pull_request]

jobs:
   stats:
      runs-on: ubuntu-latest
      steps:
         - name: Run pull request stats
           uses: flowwer-dev/pull-request-stats@master